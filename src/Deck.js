import React, { Component } from 'react'
import { View, Animated } from 'react-native'

class Deck extends Component {
  render() {
    return (
      <Animated.View>

      </Animated.View>
    )
  }
}

export default Deck